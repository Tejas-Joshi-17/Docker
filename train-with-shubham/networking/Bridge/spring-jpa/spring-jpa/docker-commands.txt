# build
docker build -t bridge-network-docker:1.0.0 .

# run
docker run --network my-custom-network --name bridge-network-docker bridge-network-docker:1.0.0
docker run --network host --rm --name bridge-network-docker bridge-network-docker:1.0.0

# logs persist
docker run -v "F:\Docker\train-with-shubham\networking\Bridge\spring-jpa\spring-jpa\image-perisist-logs":"/bridge-network-docker/logs"  --network my-custom-network --rm --name "bridge-network-docker" bridge-network-docker:1.0.0
docker run -v "F:\Docker\train-with-shubham\networking\Bridge\spring-jpa\spring-jpa\image-perisist-logs":"/bridge-network-docker/logs"  --network host --rm --name "bridge-network-docker" bridge-network-docker:1.0.0
